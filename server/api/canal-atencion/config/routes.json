{
  "routes": [
    {
      "method": "GET",
      "path": "/canal-atencions",
      "handler": "canal-atencion.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/canal-atencions/count",
      "handler": "canal-atencion.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/canal-atencions/:id",
      "handler": "canal-atencion.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/canal-atencions",
      "handler": "canal-atencion.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/canal-atencions/:id",
      "handler": "canal-atencion.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/canal-atencions/:id",
      "handler": "canal-atencion.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
