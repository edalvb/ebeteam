{
  "kind": "collectionType",
  "collectionName": "regions",
  "info": {
    "name": "region"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "nombre": {
      "type": "string"
    },
    "sedes": {
      "via": "region",
      "collection": "sede"
    }
  }
}
