export interface Departamento {
    id: String;
    codigo: String;
    nombre: String;
}

export interface Provincia {
    id: String;
    codigo: String;
    nombre: String;
}

export interface Distrito {
    id: String;
    codigo: String;
    nombre: String;
}